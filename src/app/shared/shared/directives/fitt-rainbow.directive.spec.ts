import { FittRainbowDirective } from './fitt-rainbow.directive';

describe('FittRainbowDirective', () => {
  it('should create an instance', () => {
    const directive = new FittRainbowDirective();
    expect(directive).toBeTruthy();
  });
});
